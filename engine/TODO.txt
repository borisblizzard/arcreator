ARC Legacy Executable Beta:

- implement MIDI support in XAL
- test Bitmap::rb_textSize
- fix problem with D3DX9 device reset after CTRL+ALT+Delete
- implement a window skin texture cacher to avoid constant creations of user textures for windows
- implement FULLSCREEN switch in APRIL (MAYBE for ARC Legacy Executable Beta)

other

- implement -debug argument
- add script files as internally embedded resources
- implement SHA-1 in C++
- add blend_type for Viewport
- add opacity for Viewport
- implement xal::Player::getPosition()
- hide xal::Player::getOffset()
